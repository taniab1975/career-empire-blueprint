# GitHub publication migration — 9 September 2026

## Authorisation and boundary

Tania explicitly requested moving the up-to-date Blueprint to GitHub for viewing independently of her work/two personal ChatGPT accounts and Sites credits. She also requested redirecting the previously shared ChatGPT URL to the new site when owning-account access returns. This supersedes the earlier Sites-only publishing restriction, not the requirement for one canonical Blueprint or privacy protections. No game or asset production is authorised.

Plan: preserve this canonical source and its approved records; maintain the private recovery history in taniab1975/career-empire-private-recovery; publish only the reviewed website build in a dedicated public GitHub Pages repository, career-empire-blueprint. Never publish the complete private recovery checkout. Existing public game repositories remain untouched.

Read-only preflight: the recovery repository is PRIVATE and the current user has ADMIN permission. Its production-consolidation-2026-09-08 branch exactly matches local baseline 1fc998adcfbb365dc469cb5708be75880f1c57c5. Existing main/game-snapshot branches are preserved. No existing Blueprint Pages repository appeared in the account inventory. Sites get_site for the unchanged existing project again returned 404 project_not_found; no old-site redirect is possible in this connection yet.

## Planned publication checks

Local validation completed: 30/30 data/governance/path tests, TypeScript, original production build and the new static build pass. Full desktop/mobile/keyboard smoke passes against the actual /career-empire-blueprint/ static subpath, including exact public-record content, direct links, public documents, historical playable snapshot and private-path 404 checks. Focused lint passes after scoping the Next image-optimisation rule away from the static native-image adapter (GitHub Pages has no Next image server). Public build inspection permits only approved output roots, rejects private/secret/source-map paths, and retains full file hashes. This is not a classroom integration test.

Created the dedicated public delivery repository https://github.com/taniab1975/career-empire-blueprint. Source and publication are separate: before the first Pages deployment, live GitHub parity is still pending. Private untracked asset work is preserved locally but is not swept into this migration commit.

Build a static adapter from the existing React views and canonical records, rather than rewriting the Blueprint. Preserve project-subpath navigation, hash/query deep links, all public downloads and existing visual evidence. Validate source records, TypeScript, both existing and static builds, desktop/mobile/keyboard navigation and private-path denial. Publish only the allowlisted static output, then compare live records and record the actual release identifiers.

## Deferred old-address redirect

When owning-account access returns, inspect the exact existing Sites project and current source before changing anything. Configure a server-side redirect to the verified new GitHub Pages site where supported; otherwise publish a minimal forwarding page with a visible fallback link. Preserve supported query/hash section links and explicitly map document routes. Test the root URL, decision/learning links and downloads. Public visitors cannot be redirected by changing GitHub alone: the old host must be updated and remain reachable. No redirect or automatic future task is claimed until performed.

## Maintenance

The GitHub Pages repository is a generated public delivery copy, not an editable source authority. All future tasks start from the canonical source/private mirror, log changes and visual evidence, regenerate records, run checks, rebuild and publish the reviewed static output. A site stays available without ChatGPT credits, subject to GitHub service/account availability; it does not automatically absorb future uncommitted changes or unseen chats.

Reference: https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages and https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site.
